<h1>The best menus in the universe</h1>

<%= render "menus/new_form" %>

<div id="list-of-menus">
  <% @menus.each do |menu| %>
    <%= render partial: "menus/new_menu", locals: { menu: menu } %>
  <% end %>
</div>
